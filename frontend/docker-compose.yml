# version: "3.9"

# services:

#   frontend:
#     build: .
#     container_name: enterprise-rag-frontend

#     ports:
#       - "3000:3000"

#     restart: unless-stopped

version: "3.9"

services:
  frontend:
    build:
      context: .
    container_name: enterprise-rag-frontend
    ports:
      - "3000:3000"
    networks:
      - enterprise-rag-net

    environment:
      # Safe! This is never exposed to the browser. Next.js reads it purely on the server side.
      - BACKEND_URL: http://enterprise-rag-backend:8000
      # Removed the global env_file block to keep server secrets isolated
      - BACKEND_API_KEY=${BACKEND_API_KEY}
    restart: unless-stopped

networks:
  enterprise-rag-net:
    external: true
