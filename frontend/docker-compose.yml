version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

    container_name: enterprise-rag-frontend

    ports:
      - "3000:3000"

    restart: unless-stopped
